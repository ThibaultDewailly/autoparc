# Sensitive environment variables for AutoParc Backend
# This file should be placed at: /etc/autoparc/backend.env
# Make sure to set proper permissions: chmod 600 /etc/autoparc/backend.env

# Database password (REQUIRED)
DB_PASSWORD=your_secure_database_password_here
